[project]
name = "faraday-eval"
version = "0.1.0"
description = "Offline eval adapters: local LLM-as-judge and pass@k (behind the API)"
requires-python = ">=3.10"
dependencies = ["faraday-common"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
