[project]
name = "faraday-common"
version = "0.1.0"
description = "Shared client surface for Faraday ML-plane services (stdlib-only, air-gap safe)"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
